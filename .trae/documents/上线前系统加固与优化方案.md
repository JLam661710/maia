# 执行方案：系统上线准备

感谢您的确认。我们将立即执行以下变更：

## 1. 权限与访问控制
*   **游客模式**：隐藏首页的“以游客身份开始”按钮，强制用户注册登录。
*   **注册门槛**：
    *   在 `.env` 中设置 `INVITE_CODE=MAIA2025`。
    *   在注册界面增加“邀请码”输入框，验证通过后才允许提交注册请求。
*   **管理员视图**：
    *   仅允许 `1037078681@qq.com` 和 `linxuan1037078681@gmail.com` 查看侧边栏的 Token 计数和 JSON 状态。
    *   普通用户界面完全隐藏这些调试信息。

## 2. 系统稳定性
*   **API 重试**：在 `llm_client.py` 中引入 `tenacity`，对 LLM 调用失败（如限流）的情况进行 3 次自动重试。
*   **错误反馈**：增加友好的“系统繁忙”提示。

我们将按顺序修改 `llm_client.py`, `main.py` 和 `.env` 文件。