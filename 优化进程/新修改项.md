中英文切换，去掉侧边选择栏，改为在首页的右上方单个按钮点击切换。

把 streamlit 的固定功能项，如“rerun”、settings、print，这些隐藏掉，不需要展示在前端。

既然游客模式已暂停开放 (Guest Mode Disabled)，就在首页把相关的交互组件和入口全部去除掉，不再保留。

保持首页清晰、干净、明朗、简洁。

以上都是前端界面的修改操作。不涉及核心链路的功能，不要乱改其他代码，禁止破坏程序正常运行。